# tmux.conf, veritable smorgasbord of copypasta
#
# TODO
# keyboard shortcut for select-layout even-horizontal
# command-n or alt-n for going to tabs 1-8, with 9 the very last pane


#############
# tmux basics

# unbind default prefix key (conflicts with vim)
unbind C-b

# set prefix key to ctrl-a
set -g prefix C-a

# pass through a ctrl-a if you press it twice
bind C-a send-prefix

# lower command delay
set -g escape-time 20

# start first window and pane at 1, not zero (the keyboard is not semantic)
set -g base-index 1
set -g pane-base-index 1

# what does this actually do? I don't remember
set-option -gw xterm-keys on

# history limit 10000
set -g history-limit 10000

# turn on 256 color support in tmux
set -g default-terminal "screen-256color"

# bind r to reloading the config file
bind r source-file ~/.tmux.conf \; display "Reloaded tmux config file."


################
# copy and paste

bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection


##################
# window splitting

bind c neww -c "#{pane_current_path}"

# vertical split (as per vim)
bind % split-window -h -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"

# horizontal split (as per vim)
bind "\"" split-window -v -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind s
bind s split-window -v -c "#{pane_current_path}"


############
# navigation

# navigate using vim-style keys
setw -g mode-keys vi

# use C-Tab to cycle through panes
bind -n C-Tab select-pane -t :.+
bind -n C-S-Tab select-pane -t :.-

# o for forwards, O for backwards
# iTerm2 doesn't like C-Tab etc so set that as a shortcut for C-a O
bind O select-pane -t :.-

# Alt-hjkl for pane movement (if C-hjkl gets locally over-ridden)
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# bind for Mac, remap command-hjkl to C-a-hjkl in iTerm2
bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -R

# shift-movement keys will resize panes
bind -r H resize-pane -L 10
bind -r J resize-pane -D 10
bind -r K resize-pane -U 10
bind -r L resize-pane -R 10

#################
# Vim integration

# Smart pane switching with awareness of vim splits
# see https://github.com/christoomey/vim-tmux-navigator
is_vim='echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?)(diff)?$"'
bind -n C-h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
bind -n C-j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
bind -n C-k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
bind -n C-l if-shell "$is_vim" "send-keys C-l" "select-pane -R"
bind -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"

# TODO fix the back behavior for C-Tab and C-S-Tab

# Allow us to use C-a C-l for clean screen, etc.
bind C-h send-keys 'C-h'
bind C-j send-keys 'C-j'
bind C-k send-keys 'C-k'
bind C-l send-keys 'C-l'


############
# status bar

# configure contents of status bar
set -g status-interval 1
set -g status-justify centre
set -g status-left "#[fg=green]\"#S\""
set -g status-left-length 40
set -g status-right "#[fg=green] #h | %d %b %R"
set -g status-utf8 on

# monitor activity
set -g visual-activity on
set-option -g set-titles on
setw -g automatic-rename
setw -g monitor-activity on



#######
# Linux
#
# Linux -- xclip support (commented as this often doesn't make sense on remote servers)
# bind C-c run "tmux save-buffer - / xclip -i -sel clipboard"
# bind C-v run "tmux set-buffer \"$(xclip -o -sel clipboard)\"; tmux paste-buffer"


#####
# Mac
#
# set-option -g default-command "reattach-to-user-namespace -l zsh"
# set-option -g default-command 'exec reattach-to-user-namespace -l zsh'
# bind-key C-c run-shell 'tmux save-buffer - | reattach-to-user-namespace pbcopy'
# bind-key C-v run-shell 'reattach-to-user-namespace pbpaste | tmux load-buffer - \; paste-buffer -d'
# bind-key -t vi-copy y 'reattach-to-user-namespace pbcopy'
# bind-key -t emacs-copy M-w 'reattach-to-user-namespace pbcopy'


#######
# Color

# Solarized
# https://raw.githubusercontent.com/seebi/tmux-colors-solarized/master/tmuxcolors-dark.conf
source-file ~/dotfiles/tmuxcolors-light.conf

# Zenburn
# https://raw.githubusercontent.com/plathrop/dotfiles/master/tmux/zenburn.conf
#source-file ~/dotfiles/zenburn.conf

